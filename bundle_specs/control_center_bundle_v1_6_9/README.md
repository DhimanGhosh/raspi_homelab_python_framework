# Control Center bundle spec

This spec is intended to package the current Control Center runtime as a bundle. Copy in the runtime directory from the actively working Control Center before building the TGZ.
