# Music Player v5.1.0

Fixed responsive UI, durations, playlist controls, speed dials, and playback continuation.


## v5.2.2
- Fixed container startup by launching uvicorn with `python -m uvicorn`.
- Keeps ffmpeg installed for duration extraction.
